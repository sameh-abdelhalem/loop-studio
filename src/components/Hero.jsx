const Hero = () => {
  return (
    <section id="hero">
      {/* Hero Container */}
      <div className="container max-w-6xl mx-auto px-6 py-12 md:px-0">
        {/* Menu / Logo container */}
        <nav className="flex justify-between items-center font-bold text-white"></nav>
      </div>
    </section>
  );
};

export default Hero;
